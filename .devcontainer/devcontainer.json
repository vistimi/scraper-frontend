// For format details, see https://aka.ms/devcontainer.json. For config options, see the README at:
// https://github.com/microsoft/vscode-dev-containers/tree/v0.245.2/containers/ubuntu
{
	"name": "Node",
	"build": {
		"dockerfile": "../Dockerfile",
		"args": {
			"VARIANT": "mcr.microsoft.com/devcontainers/typescript-node:16-bullseye",
			"RUNNER": "devcontainer"
		},
		"target": "runner"
	},
	// Mount the docker daemon on the container, must run outside the container as well
	// "mounts": [ "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind" ],
	"runArgs": [
		"--env-file",
		".devcontainer/devcontainer.env",
		"--net",
		"scraper-net"
	],
	// "forwardPorts": [],
	// "appPort": [],
	"initializeCommand": "if [ ! \"$(docker network ls | grep scraper-net)\" ]; then docker network create scraper-net; fi",
	"postStartCommand": "",
	"features": {},
	"customizations": {
		"vscode": {
			"extensions": [
				"yzhang.markdown-all-in-one",
				"shakram02.bash-beautify",
				"shd101wyy.markdown-preview-enhanced"
			]
		}
	}
}